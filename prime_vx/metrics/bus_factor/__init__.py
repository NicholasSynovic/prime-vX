from collections import namedtuple
from typing import List, Tuple

from prime_vx.db import (
    ANNUAL_BUS_FACTOR_DB_TABLE_NAME,
    DAILY_BUS_FACTOR_DB_TABLE_NAME,
    MONTHLY_BUS_FACTOR_DB_TABLE_NAME,
    SIX_MONTH_BUS_FACTOR_DB_TABLE_NAME,
    THREE_MONTH_BUS_FACTOR_DB_TABLE_NAME,
    TWO_MONTH_BUS_FACTOR_DB_TABLE_NAME,
    TWO_WEEK_BUS_FACTOR_DB_TABLE_NAME,
    WEEKLY_BUS_FACTOR_DB_TABLE_NAME,
)

BUCKET_STOR = namedtuple(
    typename="BUCKET_STOR",
    field_names=[
        DAILY_BUS_FACTOR_DB_TABLE_NAME,
        WEEKLY_BUS_FACTOR_DB_TABLE_NAME,
        TWO_WEEK_BUS_FACTOR_DB_TABLE_NAME,
        MONTHLY_BUS_FACTOR_DB_TABLE_NAME,
        TWO_MONTH_BUS_FACTOR_DB_TABLE_NAME,
        THREE_MONTH_BUS_FACTOR_DB_TABLE_NAME,
        SIX_MONTH_BUS_FACTOR_DB_TABLE_NAME,
        ANNUAL_BUS_FACTOR_DB_TABLE_NAME,
    ],
    defaults=[None, None, None, None, None, None, None, None],
)

INTERVAL_PAIRS: List[Tuple[str, str]] = [
    (DAILY_BUS_FACTOR_DB_TABLE_NAME, "D"),
    (WEEKLY_BUS_FACTOR_DB_TABLE_NAME, "W"),
    (TWO_WEEK_BUS_FACTOR_DB_TABLE_NAME, "2W"),
    (MONTHLY_BUS_FACTOR_DB_TABLE_NAME, "ME"),
    (TWO_MONTH_BUS_FACTOR_DB_TABLE_NAME, "2ME"),
    (THREE_MONTH_BUS_FACTOR_DB_TABLE_NAME, "QE"),
    (SIX_MONTH_BUS_FACTOR_DB_TABLE_NAME, "2QE"),
    (ANNUAL_BUS_FACTOR_DB_TABLE_NAME, "YE"),
]
